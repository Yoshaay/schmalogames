import { GameEntry } from '../core/game';
import { applausometerEntry } from './applausometer';

// Neue Minigames hier registrieren — Wall und Operator lesen beide diese Liste.
export const games: GameEntry[] = [applausometerEntry];
